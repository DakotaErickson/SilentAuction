# Silent Auction

This is a quickly thrown together project to support a silent auction at an elementary school.

To run the project locally, after install the requirements, use this command:

```python -m uvicorn app.main:app --reload```